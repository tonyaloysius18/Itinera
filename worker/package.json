{
  "name": "itinera-nera-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/auth.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
